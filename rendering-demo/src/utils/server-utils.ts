import "server-only"

export const serverSidefunction = () => {
    console.log(`use multiple libraries, use environment variables,
interact with database, process confidational information`);
    return "server result..."

}